# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

jobs:
  hello:
    machine: true
    steps:
      - run: echo "Hello, world!"
      - run: echo 'This is message in CircleCI worker'
      - run: pwd
      - run: uname -a
      - run: ps
      - run: ls
      - run: "curl -X POST -H 'Content--type: application/json' --data '{\"text\":\"Hello, Kyungim!\"}' https://hooks.slack.com/services/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"

workflows:
  welcome:
    jobs:
      - hello
